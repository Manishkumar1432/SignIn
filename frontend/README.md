# Sigin Frontend

This is a small React + MUI frontend for the sign-in page.

Quick start:

1. cd frontend
2. npm install
3. npm start

Default expects backend at http://localhost:5000 (see backend README)

Demo credentials after seeding backend: test@example.com / Password123
